/// <reference path="../../../typings/tsd.d.ts" />
'use strict';

namespace Main {
  export enum PlatformSurfaceType {
    STATIC,
    CONVEYOR,
    ICY,
    SPRINGY
  }
  
  export enum PlatformMotionType {
    STATIONARY,
    MOVING
  }
}
